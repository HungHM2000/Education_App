import {StyleSheet, Text, View} from 'react-native';
import React, {useState, useCallback, useRef} from 'react';

export default function YoutubeComponent() {
  return (
    <View>
      <Text>YoutubeComponent</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
